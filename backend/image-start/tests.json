{
  "tests": [
    {"name": "OPTIONS preflight", "method": "OPTIONS", "path": "/", "expectedStatus": 200},
    {"name": "POST без сессии — 401", "method": "POST", "path": "/", "body": {"prompt": "test"}, "expectedStatus": 401}
  ]
}
